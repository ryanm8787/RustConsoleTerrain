use rand::Rng;

struct Generator {
    max_height : i32,
    max_width : i32,
    current_gradient : i32,
}

impl Generator {

    fn update_gradeint(&self)
    {
        let mut rng = rand::thread_rng();
            
        return 0.0;
    }

}